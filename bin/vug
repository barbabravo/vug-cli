#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('init', '初始化前端项目目录')
  .command('start', '执行QBS')
  .command('upgrade', '更新QBS工具')
  .parse(process.argv)